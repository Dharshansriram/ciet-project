# Node.js sandbox for safe code execution
# Minimal alpine image — no npm registry access, no network
FROM node:18-alpine
WORKDIR /app
# No additional packages needed — vanilla Node.js only
CMD ["sh"]
